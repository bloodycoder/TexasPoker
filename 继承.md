```
Object.prototype.extend = function (args) {
    var c = {};
    for (var i in this) {
        if (typeof this[i] === 'object') {
            c[i] = (this[i].constructor === Array) ? [] : {};
            deepCopy(this[i], c[i]);
        } else {
            c[i] = this[i];
        }
    }
    for (var i in args) {
        c[i] = args[i];
    }
    return c;
};
var Chinese = {
    nation: "中国",
    career: "nope",
    func: function () {
        console.log("my career is " + this.career);
    }
};
var Doctor = Chinese.extend({
    career: "医生"
});
mydoc = Object.create(Doctor);
mydoc.func();
```
